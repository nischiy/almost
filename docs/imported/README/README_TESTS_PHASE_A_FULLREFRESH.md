README_TESTS_PHASE_A_FULLREFRESH.md
=========================================
Phase A (full refresh) suite:
- Fully offline (stubs network).
- Safe imports for Python 3.13 when parent pkgs are missing.
- Light, fast checks of indicators, risk, telemetry, backtest availability.

Run:
    pytest -q -vv
